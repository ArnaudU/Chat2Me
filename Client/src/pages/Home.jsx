import React from 'react';
import ListMessage from '../components/ListMessage';

const Home = (props) => {
    return (
        <div className='home'>
            <ListMessage />
        </div>
    );
};

export default Home;