import React from 'react';
import SendMessage from '../components/SendMessage';
const Tweet = () => {
    return (
        <div>
            <SendMessage />
        </div>
    );
};

export default Tweet;